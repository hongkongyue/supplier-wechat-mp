import { 
  categoryCache, 
  locationCityCache,
} from '@/assets/js/cache';

const state = {
        categoryList: [],          // 大分类列表
        locationCity: [],          // 定位城市
        indexList   : [1,2,3,4],   //首页的指引列表
};
export default state;
